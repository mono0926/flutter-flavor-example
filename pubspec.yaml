name: flavor_example
description: A new Flutter application.
version: 1.0.2
environment:
  sdk: '>=2.1.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: any
  enum_to_string: any
  simple_logger: any
  package_info: any
  provider: any
  firebase_auth: any
  cloud_firestore: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono:
  flutter_launcher_icons:
    git:
      url: https://github.com/fluttercommunity/flutter_launcher_icons.git
      ref: flavors
flutter:
  uses-material-design: true
# flutter_launcher_iconsはflavorで指定しているが、この指定も併記しないと動かないのでワークアラウンド
flutter_icons:
  android: true
  ios: true